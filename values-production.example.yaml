# Copy this file to values-production.yaml and replace all CHANGE_ME values.
# Do not commit real production credentials.

global:
  imageRegistry: gengbo
  defaultStorageClass: local-path
  security:
    # Required by current Bitnami charts when using a mirrored/private registry.
    allowInsecureImages: true

mysql:
  image:
    tag: 9.4.0-debian-12-r1 # MySQL 9.4.0
  auth:
    rootPassword: root
    password: root
  primary:
    service:
      type: LoadBalancer

redis:
  image:
    tag: 8.2.1 # Redis 8.2.1
  auth:
    password: ""
  master:
    service:
      type: LoadBalancer

postgresql:
  image:
    tag: 17.6.0-debian-12-r4 # PostgreSQL 17.6.0
  auth:
    postgresPassword: postgres
    password: postgres
  primary:
    service:
      type: LoadBalancer
